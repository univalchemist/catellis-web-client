import React from 'react';
import BodyClassName from 'react-body-classname';

export const MarketingAuthLayout = ({children}) => (
  <BodyClassName className="marketing-auth">
    {children}
  </BodyClassName>
)

export default MarketingAuthLayout
